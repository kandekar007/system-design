package org.example.designpatterns.observer;

public interface Channel {
    /**
     * # An observable is an object which notifies observers about the changes in its state.
     */


    void setNews(Object o);
}
